// Placeholder JS for interactive buttons (Add to cart, menu, etc.)
document.querySelectorAll('.product-info button').forEach(btn => {
  btn.addEventListener('click', () => {
    alert('Added to cart!');
  });
});
// Add more interactivity if needed here.
